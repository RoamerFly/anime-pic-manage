# ADR-0002：所有文件整理先生成 dry-run 计划

- 状态：已接受（V0.1 及以后）
- 日期：2026-09-02

## 背景

角色识别可能低置信度或错误，自动移动、改名和删除会造成不可逆损失。用户必须能逐项检查目标路径、状态和冲突。

## 决定

扫描/识别阶段只写结果和分类计划。真实移动/复制/重命名须由用户明确确认，执行前重新校验源指纹、目标范围和冲突。跨卷移动采用复制→校验→删除源。删除优先回收站并需人工确认。

## 结果

计划和实际操作可审计、可重试、可撤销；实现复杂度增加，但数据安全优先于自动化完成率。
