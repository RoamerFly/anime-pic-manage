# ADR-0003：模型权重不进入 Git 或默认安装包

- 状态：已接受
- 日期：2026-09-02

## 背景

模型体积大，上游访问条件和许可证可能变化，且不同模型的分发许可不同。将权重提交或无条件打包会增加仓库、供应链和合规风险。

## 决定

Git 只保存模型目录说明、manifest 示例和来源链接。用户主动下载/导入后，安装器校验 revision、元数据、许可证提示和 SHA-256，并在本地锁定。应用发布默认不内置受控权重。

## 结果

仓库更小、模型可替换且分发边界清晰；首次使用需要额外下载和检查，离线用户需要按文档准备模型。
