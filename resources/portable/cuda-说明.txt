这个文件夹存放显卡加速所需的 CUDA 12 / cuDNN 9 运行库。

完整本地 GPU 构建与独立 CUDA 分包会从 NVIDIA 官方 PyPI wheel 准备
CUDA 12、cuBLAS、cuFFT 与 cuDNN 9，不需要安装系统 CUDA。对应许可文件
位于本目录的 licenses\ 子目录；轻量程序包也可在设置页自动准备。

CPU 包默认不附带这些大文件；可打开应用 →「设置 → 基础配置 →
推理运行环境」→「一键下载 CUDA 运行时」，同样会保存到这里。

如果关键 DLL 缺失，应用会自动回落到 CPU 推理，识别功能不受影响。
