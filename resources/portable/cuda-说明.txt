这个文件夹存放显卡加速所需的 CUDA 12 / cuDNN 9 运行库。

怎么获得：打开应用 →「设置 → 基础配置 → 推理运行环境」→
点「一键下载 CUDA 运行时」（约 1.4GB）。下载完成后会自动解压到这里，
并把该目录加入推理引擎的搜索路径，不需要在系统里安装 CUDA。

为什么是下载而不是随包附带：这些运行库属于 NVIDIA 的专有组件，
由应用按需从 PyPI 获取，避免在发布包里二次分发。

如果这个文件夹是空的，应用会自动回落到 CPU 推理，识别功能不受影响。
